doctype html 
html
  head
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    title #{title}
    link(rel='stylesheet', href='https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css')
    link(rel='stylesheet', href="style1.css")
  body
    .container
      h1 標準の場所の編集
      p.text-center.text-info folders.txt を編集しメニューバーに表示される場所を変更します。
      .menu
        a.p-3(href="javascript: window.close()") フォームを閉じる
      form.mt-4(method="POST")
        label.form-label
          | 場所 (folders.txt) <span style="color:red;">(注意) フォルダ名の最後は必ず '/' で終了してください。</span>
          textarea.form-control(name="places", id="places", cols="80", rows="6") #{content}
        br
        button.btn.btn-primary(type="submit") 保存する
      p.message #{message}
